/**
 * @deepseek-ai/dsh-efai-base — every row this fork adds to a base-backed
 * profile, as a profile bundle. The package's substance is
 * `cordis.patch.yml`, declared by the `dsh.bundle.patch` manifest field and
 * resolved by the profile composer through that field; this module carries no
 * runtime API.
 * @module @deepseek-ai/dsh-efai-base
 */

export {}
